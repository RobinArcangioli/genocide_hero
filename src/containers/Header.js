import logo from '../images/logo512.png';

const Header = () => {
  return (
    <div className="header">
      <img src={logo} className="appLogo" alt="logo" width="100" />
      GENOCIDE HERO
    </div>
  );
};

export default Header;
